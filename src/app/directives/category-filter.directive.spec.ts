import { CategoryFilterDirective } from './category-filter.directive';

describe('CategoryFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryFilterDirective();
    expect(directive).toBeTruthy();
  });
});
